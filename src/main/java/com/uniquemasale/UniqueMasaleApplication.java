package com.uniquemasale;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Unique Masale &amp; Food Products web application.
 *
 * <p>Run locally with: {@code mvn spring-boot:run} and open
 * <a href="http://localhost:8080">http://localhost:8080</a>.</p>
 */
@SpringBootApplication
public class UniqueMasaleApplication {

    public static void main(String[] args) {
        SpringApplication.run(UniqueMasaleApplication.class, args);
    }
}
